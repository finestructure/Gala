import XCTest
@testable import PreviewProviderTools

final class PreviewProviderToolsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PreviewProviderTools().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
